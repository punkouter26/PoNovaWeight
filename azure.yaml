# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: PoNovaWeight
metadata:
  template: PoNovaWeight
infra:
  provider: bicep
  path: ./infra
services:
  app:
    language: dotnet
    project: ./src/PoNovaWeight.Api
    host: appservice

